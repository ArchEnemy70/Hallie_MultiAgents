<Window x:Class="Hallie.Views.Approvals"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hallie.Views"
        mc:Ignorable="d"
        Title="Validation requise" Height="380" Width="650"
        WindowStartupLocation="CenterOwner" Background="#F0F0F0">
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" FontSize="16" FontWeight="Bold" Grid.Row="0"/>

        <TextBox x:Name="SummaryText" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>

        <Expander Header="Détails techniques (JSON)" IsExpanded="False" Margin="0,10,0,0" Grid.Row="2">
            <TextBox x:Name="PayloadText" Grid.Row="2" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
        </Expander>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Refuser" Width="90" Margin="0,0,8,0" Click="Reject_Click"/>
            <Button Content="Valider" Width="90" Click="Approve_Click"/>
        </StackPanel>
    </Grid>
</Window>
