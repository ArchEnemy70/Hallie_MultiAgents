<Window x:Class="Hallie.Helpers.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hallie.Helpers"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Width="300" MinWidth="300" MaxWidth="300"         
        MinHeight="150"
        SizeToContent="WidthAndHeight"
        Loaded="Window_Loaded">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                 From="0" To="1" Duration="0:0:0.5" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
    <Border CornerRadius="12"
            Background="#CC1E1E1E"  
        BorderBrush="White"
            BorderThickness="1"
            Padding="15">
        <StackPanel>
            <TextBlock x:Name="Titre"
                       Text="🔔 Notification"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0 0 0 5"/>
            <TextBlock x:Name="Message"
                       Text="Cliquez sur un bouton pour continuer."
                       Foreground="White"
                       TextWrapping="Wrap"
                        TextTrimming="None"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top" 
                       Margin="0 0 0 15"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Editer" Width="80" Margin="5" Click="Edit_Click"/>
                <Button Content="Ok" Width="80" Margin="5" Click="Ok_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
